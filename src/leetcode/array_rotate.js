
/**
 *
 *
 * @param {*} matrix
 */



/**
 * @param {number[][]} matrix
 * @return {void} Do not return anything, modify matrix in-place instead.
 */
var rotate = function (matrix) {
    for (let i = 0, ml = matrix.length; i < ml; i++) {
        let row = matrix[i]
        for (let j = 0, rl = row.length; j < rl; j++) {
            // ....真的不会做 
        }
    }
};